// Cloudflare Workers static assets: the static export in out/ is the whole
// site, with no Worker code. Built and deployed by Workers Builds on every
// push to main (PROJECT.md §15).
{
  "name": "transitlateagain",
  "compatibility_date": "2026-09-24",
  "assets": {
    "directory": "./out",
    "not_found_handling": "404-page"
  }
}
